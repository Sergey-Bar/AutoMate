// Tests for ConversationSidebar are in conversation-sidebar.test.tsx
// This file exists to satisfy vitest "No test suite found" requirement.
import { describe, it } from 'vitest';
describe('conversation-sidebar stubs', () => {
  it('is covered by conversation-sidebar.test.tsx', () => {
    // All meaningful tests are in the .tsx file which supports JSX
  });
});
