# Capability Register

**Baseline:** `01cc19c8167e0e8365928862de463fd50caaee5c`
**Scope:** current unified repository, before classified working-tree changes
**Source of truth:** this register, not historical readiness documents

## Status vocabulary

- `real` — implemented and exercised, with the stated qualification.
- `mock` — a route, response, or UI exists but does not provide the target behavior.
- `missing` — no usable implementation exists in the target.
- `deferred` — intentionally outside the launch scope pending a separate decision.
- `obsolete` — retained only as historical residue and not part of the target product.

A qualification is not a substitute for a status. Split a capability when its pieces have different truth values.

## Register

| ID                           | Capability                                            | Status   | Qualification                                                                                                                                             | Evidence                                                                                                                | Target phase      |
| ---------------------------- | ----------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ----------------- |
| reporting.lifecycle          | Reporter lifecycle ingestion                          | real     | Canonical v2 contract, Playwright/JUnit adapters, legacy normalization, and duplicate-safe ingestion exist; full producer breadth remains bounded.        | `packages/reporter/`; `apps/api/src/services/reporter-ingestion.ts`                                                     | 2                 |
| reporting.persistence        | Run and test persistence                              | real     | Drizzle repository exists when configured; the composition also permits an in-memory development fallback.                                                | `apps/api/src/repositories/drizzle-run-repository.ts`; `apps/api/src/index.ts:34-41`                                    | 3                 |
| reporting.explorer           | Run list, detail, quarantine, and quality gates       | real     | Canonical result routes and RunExplorer exist; durable database projections and recursive step/evidence views remain incomplete.                          | `apps/api/src/routes/reporting.ts`; `apps/web/src/components/dashboard/RunExplorer.tsx`                                 | 4                 |
| reporting.analytics          | Basic dashboard analytics                             | real     | Run-oriented summaries exist; population, proof, completeness, and denominator rules are not canonical.                                                   | `apps/api/src/modules/dashboard/analytics.ts`                                                                           | 4                 |
| reporting.advanced-analytics | Trends, leaderboards, and performance dashboards      | mock     | Several pages and calculations are hard-coded or disconnected from canonical run evidence.                                                                | `apps/web/src/routes/dashboard/`; `apps/web/src/components/dashboard/`                                                  | 4                 |
| realtime.sse                 | Live run updates over SSE                             | real     | Versioned envelopes, cursor-based process-local replay, and a PostgreSQL feed boundary exist; API composition still uses the local bus.                   | `packages/realtime/`; `apps/api/src/realtime/realtime-bus.ts`                                                           | 3                 |
| artifacts.storage            | Binary artifact storage and retrieval                 | mock     | A local atomic filesystem store with digest verification exists; production S3/object-store ownership and retention are not wired.                        | `apps/api/src/infrastructure/artifact-store.ts`                                                                         | 3                 |
| auth.sessions                | Revocable installation sessions                       | mock     | HttpOnly cookie routes, HMAC-hashed tokens, expiry, revocation, and a Drizzle session store exist; two-instance and retention evidence remain.            | `apps/api/src/routes/auth.ts`; `packages/auth/src/credentials.ts`; `packages/db/src/repositories/session-repository.ts` | 3                 |
| auth.service-credentials     | Scoped service credentials and runner identities      | mock     | Identity schema and one-time enrollment control routes exist; durable key rotation, database-backed leases, and recovery are not proven.                  | `packages/db/src/schema/identity.ts`; `apps/api/src/routes/runner.ts`                                                   | 3, 5              |
| contracts.authority          | One shared contract authority                         | real     | API, web, reporter, reporting, realtime, and orchestration use the shared Zod package; legacy schemas remain only as compatibility exports.               | `packages/shared-contracts/src/`; `packages/reporter/`; `apps/web/src/lib/api.ts`                                       | 2                 |
| db.postgres-target           | PostgreSQL target schema and forward migrations       | real     | Drizzle PostgreSQL schema and migration metadata exist; target-domain rollout and migration reconciliation are incomplete.                                | `packages/db/src/schema/dashboard.ts`; `packages/db/drizzle/`                                                           | 3                 |
| orchestration.inventory      | Automation inventory and schedules                    | mock     | Canonical definitions, schedules, jobs, and API routes exist in a process-local service; durable scheduler tables and two-worker proof are pending.       | `packages/orchestration/`; `apps/api/src/routes/orchestration.ts`                                                       | 5                 |
| orchestration.leases         | Atomic job leases and fencing                         | mock     | Runner control enforces fencing tokens and ordered events in memory; PostgreSQL lease/reaper guarantees are not implemented.                              | `apps/api/src/services/runner-control.ts`; `packages/runner-sdk/`                                                       | 5                 |
| runner.oci                   | Independently deployable OCI runner                   | mock     | Runner SDK, rootless execution interfaces, manifests, and local Dockerfile exist; image digests are unbuilt and Podman execution is unavailable.          | `apps/runner/`; `packages/runner-sdk/`; `runners/`                                                                      | 5                 |
| tool.playwright              | Isolated Playwright tool execution                    | mock     | Reporter ingestion exists, but no isolated runner, pinned image, or complete attempt model exists.                                                        | `apps/api/src/routes/reporter.ts`; `packages/shared-contracts/src/schemas/reporter-events.ts`                           | 2, 5              |
| tool.junit                   | JUnit producer adapter                                | real     | Bounded SAX parsing, status mapping, and adapter tests exist.                                                                                             | `packages/reporter/src/adapters/junit-xml.ts`; `packages/reporter/src/junit.test.ts`                                    | 2                 |
| tool.k6                      | k6 metric and threshold adapter                       | missing  | No k6 parser, threshold model, or isolated image exists.                                                                                                  | `packages/`; `runners/`                                                                                                 | 2, 5              |
| tool.zap                     | ZAP finding and SARIF adapter                         | missing  | No ZAP adapter or security-finding projection exists.                                                                                                     | `packages/`; `runners/`                                                                                                 | 2, 5              |
| automation.chat              | Provider-backed chat                                  | mock     | Chat routes return deterministic in-process responses rather than a dynamic provider stream.                                                              | `apps/api/src/modules/orchestrator/chat.ts`                                                                             | 6                 |
| automation.test-generation   | AI test generation                                    | mock     | Generated assertions are static and lack persisted tool-run provenance.                                                                                   | `apps/api/src/modules/orchestrator/test-gen.ts`                                                                         | 6                 |
| automation.agents            | Browser/API/load/security/mobile agents               | mock     | Agent endpoints return static completed outputs and do not execute isolated tools.                                                                        | `apps/api/src/modules/agents/`                                                                                          | 5, 6              |
| ai.catalog                   | Dynamic Kilo model catalog and gateway                | mock     | Dynamic Kilo and Ollama adapters exist, but API/profile persistence and approval flow are not wired.                                                      | `packages/automation/src/kilo-gateway.ts`; `packages/automation/src/ollama-gateway.ts`                                  | 6                 |
| connectors.core              | GitHub, Jira, and Slack execution                     | mock     | Bounded adapters and SDK tests exist, but API composition, durable credentials, approvals, and artifacts are not wired.                                   | `packages/connectors/`; `apps/api/src/modules/connectors/`                                                              | 7                 |
| vault.persistence            | Version-encrypted vault storage                       | mock     | Versioned AES-GCM envelope and local artifact store exist, but database-backed key rotation and audit integration are incomplete.                         | `apps/api/src/infrastructure/vault-crypto.ts`; `apps/api/src/infrastructure/artifact-store.ts`                          | 3, 7              |
| connectors.mcp               | MCP gateway and management                            | deferred | No accepted launch contract; MCP UI/server surfaces are not part of the target core.                                                                      | `apps/api/src/modules/`; `apps/web/src/routes/automate/`                                                                | Separate ADR      |
| quality.suppression          | Fingerprinted suppression policy                      | deferred | No accepted suppression data contract or fail-closed product flow exists.                                                                                 | `packages/`                                                                                                             | Separate decision |
| ui.dashboard                 | Product dashboard and KPI cockpit                     | real     | Run-oriented dashboard routes exist; canonical evidence, runner, job, and KPI drill-through views are incomplete.                                         | `apps/web/src/routes/dashboard/`; `apps/web/src/components/dashboard/`                                                  | 4                 |
| ui.legacy-surfaces           | Webwright, MCP, legacy automation, and admin surfaces | obsolete | Webwright code and deployment references were removed; remaining legacy routes are not launch capability.                                                 | `docs/migration/source-manifest.json`; `docs/migration/phase-0-decision-record.md`                                      | 1, 10             |
| desktop.tauri                | Tauri desktop application                             | obsolete | Source shell is incomplete and explicitly outside launch scope.                                                                                           | `AutoMate/apps/desktop/` reference only                                                                                 | 10                |
| migration.tool               | Resumable source migration and reconciliation         | mock     | Deterministic plan, checkpoint, catalog, artifact inventory, vault reference, and reconciliation primitives exist; real source waves are not applied.     | `tools/migrate-cli/src/plan.ts`; `tools/migrate-cli/src/state.ts`; `tools/migrate-cli/src/reconcile.ts`                 | 8                 |
| governance.commands          | One root governance command surface                   | real     | Root format, lint, typecheck, unit, contract, integration, runner, migration, build, Drizzle, coverage, and security commands execute from the workspace. | `package.json`; `turbo.json`; `.github/workflows/unified-ci.yml`                                                        | 1                 |
| governance.boundary          | Automated repository-boundary validation              | real     | Preflight enforces root-only legacy exclusions, nested repository/config detection, generated output, test markers, and pinned workflows.                 | `scripts/unify-preflight.mjs`; `.gitignore`                                                                             | 1                 |

## Evidence rules

- A `real` row must cite both implementation and executable test evidence when the capability is user-visible or security-sensitive.
- A `mock` row must identify the boundary that makes it non-production.
- A `missing`, `deferred`, or `obsolete` row must cite the accepted plan decision or the absent implementation.
- Source repository evidence is provenance only; it does not change the target status.
- Updating this register requires re-running the relevant focused tests and recording the evidence in the Phase change.

## Current launch truth

The launch surface is the local/sample-only unified target. The first end-to-end product slice is canonical reporter ingestion through the reporting API/RunExplorer, with runner and migration boundaries available for local rehearsal. OCI execution, browser E2E auth, and production data/publication remain explicitly blocked.
